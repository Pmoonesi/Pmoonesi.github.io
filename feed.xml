<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://pmoonesi.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://pmoonesi.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2025-07-22T21:24:55+00:00</updated><id>https://pmoonesi.github.io/feed.xml</id><title type="html">blank</title><subtitle>A simple, whitespace theme for academics. Based on [*folio](https://github.com/bogoli/-folio) design. </subtitle><entry><title type="html">Zotero Links Not Working in Obsidian? It Might Be Snap</title><link href="https://pmoonesi.github.io/blog/2025/obsidian_zotero_link/" rel="alternate" type="text/html" title="Zotero Links Not Working in Obsidian? It Might Be Snap"/><published>2025-07-22T00:00:00+00:00</published><updated>2025-07-22T00:00:00+00:00</updated><id>https://pmoonesi.github.io/blog/2025/obsidian_zotero_link</id><content type="html" xml:base="https://pmoonesi.github.io/blog/2025/obsidian_zotero_link/"><![CDATA[<p>In this blog post, I explain how I managed to connect Zotero to Obsidian on Ubuntu 22.04. I am trying to officially get into the academic research world and it is the first time that I am supposed to do literature review. Knowing that things could get too complicated too quickly, I decided to seek a few tools that could help me organize the literature I’m reading. A quick search in YouTube led me to Obsidian and during the search I figured out about the Zotero integration community plugin it has. Getting started with this <code class="language-plaintext highlighter-rouge">integration</code> had a few challenges for me that I discuss in the rest of this post.</p> <p>I’m using Ubuntu 22.04 and had Zotero previously installed. I also installed Obsidian using snap in the beginning. To set up both Zotero and Obsidian you need to install <a href="https://retorque.re/zotero-better-bibtex/">Better BibTeX (BBT)</a> plugin for Zotero and also <a href="https://github.com/mgmeyers/obsidian-zotero-integration">Zotero Integration</a> for Obsidian. I did both of these following this awesome video:</p> <iframe style="display: block; margin: 1rem auto; width: 600px; height: 400px;" src="https://www.youtube.com/embed/zEYp0BJL7MU?si=SvMYPnAAFhngCvZA" title="YouTube video player" frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share" referrerpolicy="strict-origin-when-cross-origin" allowfullscreen=""></iframe> <p>After setting everything up and importing my highlights from Zotero, I noticed that the links to Zotero items weren’t working; by that I mean clicking them from within Obsidian did nothing. To resolve the issue I did a couple of steps which might help anyone who is struggling to get this going.</p> <p>First, I tried to find if there were errors relating this issue, since I did not get any warnings.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>~ journalctl <span class="nt">--since</span> <span class="s2">"5 minutes ago"</span> | <span class="nb">grep </span>obsidian
~ journalctl <span class="nt">--since</span> <span class="s2">"5 minutes ago"</span> | <span class="nb">grep </span>zotero
</code></pre></div></div> <p>the response was always from Obsidian and nothing from Zotero, so I gave up on Zotero errors:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Jul 21 12:46:05 your-host obsidian_obsidian.desktop[8139]: Opening URL: zotero://open-pdf/library/items/8DNCPSUA?page<span class="o">=</span>9&amp;annotation<span class="o">=</span>PEYX9TKS
Jul 21 12:46:05 your-host obsidian_obsidian.desktop[57602]: gio: zotero://open-pdf/library/items/8DNCPSUA?page<span class="o">=</span>9&amp;annotation<span class="o">=</span>PEYX9TKS: The specified location is not supported
</code></pre></div></div> <p>you can recognize the error:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>gio: zotero://open-pdf/library/items/8DNCPSUA?page<span class="o">=</span>9&amp;annotation<span class="o">=</span>PEYX9TKS: The specified location is not supported
</code></pre></div></div> <p>you can replicate this error like the following:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>~ xdg-open dfsdsf://salam
gio: dfsdsf://salam: The specified location is not supported
</code></pre></div></div> <p>which indicates one thing; My computer doesn’t understand the <code class="language-plaintext highlighter-rouge">zotero://</code> protocol. There could be a few root causes:</p> <ol> <li>I have not told my computer what <code class="language-plaintext highlighter-rouge">zotero://</code> means. or</li> <li>I’ve defined it, but Obsidian has no access to the definitions.</li> </ol> <p>To make sure it is not the first reason, I had to register a handler for Zotero mime type. To do that I navigated to ~/.local/share/applications where you usually have your <code class="language-plaintext highlighter-rouge">.desktop</code> files. I could even see my <code class="language-plaintext highlighter-rouge">zotero.desktop</code> there which looked like this:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="o">[</span>Desktop Entry]
<span class="nv">Name</span><span class="o">=</span>Zotero
<span class="nv">Exec</span><span class="o">=</span>bash <span class="nt">-c</span> <span class="s2">"</span><span class="si">$(</span><span class="nb">dirname</span> <span class="si">$(</span><span class="nb">realpath</span> <span class="si">$(</span><span class="nb">echo</span> %k | <span class="nb">sed</span> <span class="nt">-e</span> <span class="s1">'s/^file:\\/\\///'</span><span class="si">)))</span><span class="s2">/zotero -url %U"</span>
<span class="nv">Icon</span><span class="o">=</span>/path/to/Zotero_linux-x86_64/icons/icon128.png
<span class="nv">Type</span><span class="o">=</span>Application
<span class="nv">Terminal</span><span class="o">=</span><span class="nb">false
</span><span class="nv">Categories</span><span class="o">=</span>Office<span class="p">;</span>
<span class="nv">MimeType</span><span class="o">=</span>text/plain<span class="p">;</span>x-scheme-handler/zotero<span class="p">;</span>application/x-research-info-systems<span class="p">;</span>text/x-research-info-systems<span class="p">;</span>text/ris<span class="p">;</span>application/x-endnote-refer<span class="p">;</span>application/x-inst-for-Scientific-info<span class="p">;</span>application/mods+xml<span class="p">;</span>application/rdf+xml<span class="p">;</span>application/x-bibtex<span class="p">;</span>text/x-bibtex<span class="p">;</span>application/marc<span class="p">;</span>application/vnd.citationstyles.style+xml
X-GNOME-SingleWindow<span class="o">=</span><span class="nb">true</span>
</code></pre></div></div> <p>which as you can see, completely accounts for <code class="language-plaintext highlighter-rouge">zotero://</code> by mentioning x-scheme-handler/zotero as a <a href="https://specifications.freedesktop.org/desktop-entry-spec/0.9.5/mime-types.html">MimeType</a>, but I did not know that it is enough at the time. Here’s what I did:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>~ <span class="nb">touch </span>zotero-uri-handler.desktop

~ <span class="nb">cat</span> <span class="o">&lt;&lt;</span><span class="no">EOF</span><span class="sh"> &gt; zotero-uri-handler.desktop
[Desktop Entry]
Name=Zotero URI Handler
Exec=/path/to/Zotero_linux-x86_64/zotero -uri %u
Type=Application
NoDisplay=true
MimeType=x-scheme-handler/zotero;
</span><span class="no">EOF

</span>~ update-desktop-database ~/.local/share/applications
</code></pre></div></div> <p>which defines a new <code class="language-plaintext highlighter-rouge">.desktop</code> and then adds it to the database, which means that my system knows that it could use <code class="language-plaintext highlighter-rouge">Zotero URI Handler</code> to handle <code class="language-plaintext highlighter-rouge">x-scheme-handler/zotero</code> . Clicking on Obsidian links still did not do anything.</p> <p>I tried it another way; Now that I had the right handler in place, I tried to declare it as the main handler for <code class="language-plaintext highlighter-rouge">x-scheme-handler/zotero</code> :</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>~ xdg-mime default zotero-uri-handler.desktop x-scheme-handler/zotero
</code></pre></div></div> <p>which could be verified with:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>~ xdg-mime query default x-scheme-handler/zotero
zotero-uri-handler.desktop
</code></pre></div></div> <p>This proves that my computer knows what <code class="language-plaintext highlighter-rouge">zotero://</code> means. To verify even further I tried:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>~ xdg-open <span class="s2">"zotero://open-pdf/library/items/8DNCPSUA?page=9&amp;annotation=PEYX9TKS"</span>
</code></pre></div></div> <p>and it opened up Zotero and navigated to the exact file, page and annotation. We’ve seen this link show up in <code class="language-plaintext highlighter-rouge">journalctl</code> before. It means that Obsidian is trying to open the right link, and under right circumstances, that link actually does what we want. But, still when we click on the link in Obsidian, it doesn’t work. So, we have the right to think that maybe Obsidian doesn’t see what our computer sees about the Zotero mime type.</p> <p>Referring to <a href="https://ubuntu.com/blog/a-guide-to-snap-permissions-and-interfaces">this quote</a> about snap packages:</p> <blockquote> <p>To support this, each package is <em>sandboxed</em> so that it runs in a constrained environment, isolated from the rest of the system…</p> </blockquote> <p>I would say my problem has always been the case that Obsidian has no way of knowing what <code class="language-plaintext highlighter-rouge">zotero://</code> is and even if it did, it didn’t have access to my Zotero since it is running in a sandbox. In the final step, I uninstalled Obsidian from snap and installed the .deb package and it solved my problem. Finally I was able to navigate to the correct page and annotation of articles using links in Obsidian.</p> <p>TL;DR: If Zotero links don’t work in Obsidian on Ubuntu, and you installed Obsidian via Snap, try the <code class="language-plaintext highlighter-rouge">.deb</code> version instead; The snap sandbox might be blocking url handlers.</p> <p>Thank you for reading this post. I’d appreciate any recommendations or feedback.</p>]]></content><author><name>Parham</name></author><summary type="html"><![CDATA[In this blog post, I explain how I managed to connect Zotero to Obsidian on Ubuntu 22.04. I am trying to officially get into the academic research world and it is the first time that I am supposed to do literature review. Knowing that things could get too complicated too quickly, I decided to seek a few tools that could help me organize the literature I’m reading. A quick search in YouTube led me to Obsidian and during the search I figured out about the Zotero integration community plugin it has. Getting started with this integration had a few challenges for me that I discuss in the rest of this post.]]></summary></entry></feed>